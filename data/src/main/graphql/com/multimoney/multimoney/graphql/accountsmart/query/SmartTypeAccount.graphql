query SmartAccountType(
    $idBrand: Int!
    $user: String!
) {
    typeAccount(typeAccountInput: {
        idBrand: $idBrand
        user: $user }
    ) {
        result {
            id
            name
        }
        message
    }
}