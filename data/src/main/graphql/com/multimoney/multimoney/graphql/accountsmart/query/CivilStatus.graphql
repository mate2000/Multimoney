query CivilStatus($user: String!, $idBrand: Int!){
    civilStatus(civilStatusInput: {
            user: $user, idBrand: $idBrand
        }
    ) {
        result {
            maritalStatusId
            maritalStatusCode
            maritalStatusDescription
        }
    }
}