mutation ValidateOTP($email:String!, $otp:String!){
    valideOTP(
        valideOTPInputType: {
            email: $email,
            otp : $otp
        }
    ) {
        detail
        message
        status
    }
}