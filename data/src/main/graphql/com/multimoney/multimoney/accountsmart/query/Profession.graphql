query Profession($user:String!, $idBrand: Int!){
    profession(
        professionInput: {
            user: $user, idBrand: $idBrand
        }
    ) {
        result {
            id
            name
        }
    }
}