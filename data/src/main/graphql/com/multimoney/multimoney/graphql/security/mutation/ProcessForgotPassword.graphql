mutation ProcessForgotPassword($email:String!, $newPassword:String!, $otp:String!){
    processforgotPassword(processforgotPasswordInputType: {
        email: $email
        newPassword: $newPassword
        otp: $otp
    }
    ) {
        status
        message
        detail
    }
}