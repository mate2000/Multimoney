query validatePasswordStructure($pkUser:Int!, $user:String!, $idBrand:Int!) {
    validatePasswordStructure(validatePasswordStructureDto: {
        user: $user,
        idBrand: $idBrand,
        pkUser: $pkUser
      }) {
        data
        status
        message
        detail
      }
}