mutation ChangePhone($identification:String!, $phone:String!,$pkUser:Long!,$idBrand:Int!){
    changePhone(
        changePhoneInputType: {
            identification: $identification,
            phone:  $phone,
            pkUser: $pkUser,
            idBrand: $idBrand
        }
    ) {
        status
    }
}