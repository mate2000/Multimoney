query ProfessionSmart($pkUser:String!, $idBrand: Int!){
    professionSmart(
        professionInput: {
            user: $pkUser, idBrand: $idBrand
        }
    ) {
        result {
            id
            name
        }
    }
}