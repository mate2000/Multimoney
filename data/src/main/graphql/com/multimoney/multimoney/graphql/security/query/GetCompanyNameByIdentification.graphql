query GetCompanyNameByIdentification (
    $identification: String!,
    $idBrand: Int!,
    $user: String!
) {
    getCompanyNameByIdentification(
        dataCompanyInformationInput: {
            identification: $identification
            idBrand: $idBrand
            user: $user
        }
    ) {
        nombre
    }
}